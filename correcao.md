#Questao1
Correto
Nota: 1,00

#Questao2
Correto
Nota: 1,00

#Questao3
Correto
Nota: 1,00

#Questao4
Correto
Nota: 1,00

#Questao5
Correto
Nota: 1,00

#Questao6
Correto
Nota: 1,00

#Questao7
Parcialmente correto
Nota: 0,50

#Questao8
Parcialmente correto
Nota: 0,50

#Questao9
Correto
Nota: 1,00

#Questao10
Correto
Nota: 1,00

Subtotal: 9,00

Penalizações:
- participantes.md incompleto (integrantes sem RM): -1,00

Total final: 8,00

🧾 EXPLICAÇÃO DA CORREÇÃO

*Quando a entrega cumpre o objetivo pedido na questão, foi considerada **correta**, mesmo sem repetir o enunciado ao pé da letra.*

**Questão 1 — Ambiente:** Comentário com saídas de `node -v`, `npm -v` e `git --version`.

**Questão 2 — HTML + JS inicial:** `<h1>Checkpoint #1</h1>`, `console.log` e `alert` presentes; `script.js` referenciado antes de `</body>`.

**Questão 3 — Repositório e commit:** Comentário descrevendo o repositório no topo; histórico Git com vários commits iniciais da equipe.

**Questão 4 — Variáveis:** 2 `var`, 2 `let` e 2 `const` com `console.log` dos valores.

**Questão 5 — Aritmética:** Variáveis `a` e `b` com `+`, `-`, `*` e `/` no console.

**Questão 6 — Comparação:** Exemplos de `==`, `===`, `!=`, `!==`, `>` e `<`.

**Questão 7 — Lógicos:** Há `&&` e `||`, mas o terceiro teste usa `!=` em vez do operador `!` — parcial.

**Questão 8 — Branch e merge:** Existe `origin/feature/questao8` (commit “branch e merge exmeplo”), mas **não há PR**; o diff da branch em relação à `main` remove apenas uma linha em branco — não cumpre o pedido pedagógico. Sem comentário “Exemplo de branch e merge” no `script.js` da `main`. Parcial pela criação da branch.

**Questão 9 — compararNumeros:** Função com `if` cobrindo maior / menor / igual e testes (5,10), (10,5), (7,7).

**Questão 10 — Operadores no navegador:** Soma com rótulo e booleanos com `&&` / `||`.

*Penalizações aplicadas:* `participantes.md` no Git vinha incompleto (Lucas Alves Ferreira e Cosme Arthur sem RM). RMs completados a partir da lista de presença para o `notas.md`; a penalização de −1,00 permanece.

---

«CONTEXTO GIT»: `origin` → `lafbr/checkpoint-1-webdev-1esz26`; `main` + `feature/questao8`; diff da feature ≈ 1 linha em branco removida; sem PR.
